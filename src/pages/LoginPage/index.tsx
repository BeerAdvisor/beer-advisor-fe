import React from 'react';

import { Login } from '../../forms';

import { LoginPage, LoginWrapper } from './style';

export default () => (
    <LoginPage>
        <LoginWrapper>
            <Login />
        </LoginWrapper>
    </LoginPage>
);
