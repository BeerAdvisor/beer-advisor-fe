import styled from '../../styled-components';
import { SmallButton } from '../../components';

export const LimitedWidthButton = styled(SmallButton)`
    max-width: 15rem;
    align-self: flex-end;
`;
